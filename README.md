# coffeetalk
A small CLI application that uses configuration inputs to define LLM-backed personas that will engage in multi turn conversations with or without you and generate a directed result. It is configurable to use OpenAI-compatible API endpoints or Ollama for LLM providers.
